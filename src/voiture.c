#include <stdio.h>

struct car {
	int id;
	int pitFlag;
	int outFlag;
		
};

int main (int argc, char *argv[]) {
	
	// Loop while !raceisOver
	//
	// Generate first sector time 
	//
	// Generate second sector time
	//
	// Generate third sector time
	// isPitStop ? 

}


void writeSectorTime() {
	// Write the best sector time in the Shared Memory
}

void checkBestSectorTime() {
	// Check if the new sector time is better than the last best one (general)
}

void calcLap() {
	// Calc the total lap time
}

void writeLapTime() {
	// Write the lap time in the Shared Memory
}

int getTime() {
	// Call the time generator function
}
